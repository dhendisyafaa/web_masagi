import LayoutDashboard from "./wrapper/layoutDashboard";

export default function ConnectionSettings() {
    return (
      <LayoutDashboard>
        <div className="flex justify-center items-center w-full h-[60vh] text-primary">connection-settings page</div>
      </LayoutDashboard>
    );
}
